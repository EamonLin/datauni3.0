# 项目部署与开发说明

## 用途

本文档记录项目的开发环境和生产环境配置、常见问题及解决方案。

---

## 目录结构

```
datauni3.0/
├── saas/
│   ├── backend/          # 后端服务
│   └── frontend/         # 前端应用
└── DEPLOYMENT_NOTES.md   # 本文档
```

---

## 开发模式 vs 生产模式

### 后端启动命令

| 环境 | 命令 | 用途 |
|------|------|------|
| **开发** | `npm run dev` | 本地开发、调试、测试 |
| **生产** | `npm start` | 正式部署、稳定运行 |

### 区别

| 特性 | 开发模式 `npm run dev` | 生产模式 `npm start` |
|------|----------------------|----------------------|
| 命令 | `node --watch src/index.js` | `node src/index.js` |
| 自动重启 | ✅ 代码变化自动重启 | ❌ 不会自动重启 |
| 性能 | 较低（有监控开销） | 较高（无额外开销） |
| 稳定性 | 较低（经常重启） | 较高 |
| 适用场景 | 本地开发调试 | 线上正式环境 |

### 何时使用哪种模式？

| 场景 | 使用命令 |
|------|----------|
| 正在开发/修改代码 | `npm run dev` |
| 测试新功能 | `npm run dev` |
| 正式上线给用户用 | `npm start` |

---

## 重要注意事项

### 1. 开发模式下代码修改会自动生效

- 使用 `npm run dev` 启动后，修改代码文件会自动重新加载
- 无需手动重启服务

### 2. 生产模式下修改代码后必须重启

- 使用 `npm start` 启动后，修改代码**不会**自动生效
- 必须手动重启服务才能应用新代码

### 3. 前后端都需要启动

| 服务 | 端口 | 启动命令 |
|------|------|----------|
| 后端 | 3000 | `cd saas/backend && npm run dev` |
| 前端 | 5173 | `cd saas/frontend && npm run dev` |

---

## 生产环境部署流程

### 1. 编译前端

```bash
cd saas/frontend
npm run build
```

编译后的静态文件会生成在 `dist/` 目录。

### 2. 部署前端

将 `dist/` 目录部署到静态文件服务器（如 Nginx）。

### 3. 启动后端

```bash
cd saas/backend
npm start
```

### 4. 更新代码后的操作

```bash
# 1. 拉取最新代码
git pull

# 2. 安装依赖
cd saas/backend && npm install
cd ../frontend && npm install

# 3. 编译前端
cd saas/frontend
npm run build

# 4. 重启后端服务
cd saas/backend
# 如果使用 PM2：
pm2 restart "backend"
# 或者手动重启：
pkill -f "node src/index.js"
npm start
```

---

## 常见问题

### Q1: 为什么修改了代码，但还是旧行为？

**原因**：后端使用了生产模式 `npm start`，代码修改后不会自动重启。

**解决方案**：
- 开发时使用 `npm run dev`（自动重启）
- 或手动重启后端服务

### Q2: 商品名称大小写不同匹配不上？

**原因**：早期版本没有大小写转换逻辑。

**解决方案**：
- 当前版本已添加 `toLowerCase()` 处理
- 确保后端使用的是最新代码（开发模式或已重启）

### Q3: 同步功能没效果？

**排查步骤**：
1. 检查映射表中是否有对应的映射关系
2. 检查订单的 `product_unified_name` 是否已被设置（同步只更新 NULL 的订单）
3. 清空订单统一名称后重新同步
4. 确认后端已重启（使用最新代码）

---

## 记录历史

### 2026-05-14
- 明确了开发模式和生产模式的区别
- 添加了生产环境部署流程
- 添加了常见问题排查指南

---

## 维护说明

本文档由开发团队维护，每次重要的环境配置变更或发现新的问题解决方案时，请更新本文档。
